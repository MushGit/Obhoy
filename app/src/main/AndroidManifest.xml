<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Telemetry & Hardware Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Sensor Hardware Features -->
    <uses-feature
        android:name="android.hardware.sensor.barometer"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="true" />

    <!-- SMS Dispatch Permissions -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Evidence Vault Audio Recording Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Background Execution & Haptics -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".ObhoyApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/obhoy"
        android:roundIcon="@drawable/obhoy"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Obhoy"
        tools:targetApi="34">

        <!-- Entry Onboarding & Setup Activity -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Status Dashboard -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false" />

        <!-- Manage Emergency Contacts Activity -->
        <activity
            android:name=".ui.profile.ManageContactsActivity"
            android:exported="false"
            android:label="Manage Contacts" />

        <!-- Update Security PINs Activity -->
        <activity
            android:name=".ui.profile.UpdatePinsActivity"
            android:exported="false"
            android:label="Update Security PINs" />

        <!-- Active Escort Countdown Screen -->
        <activity
            android:name=".ui.escort.ActiveEscortActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <!-- Decoy Safe Screen (Coercion Defense Landing) -->
        <activity
            android:name=".ui.escort.DecoySafeActivity"
            android:exported="false" />

        <!-- Core Foreground Dispatch Service -->
        <service
            android:name=".service.ObhoyForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location|microphone" />

        <!-- Active Escort Dead-Man's Switch Timer Service -->
        <service
            android:name=".service.ActiveEscortTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Hardware Power Button Quad-Click Accessibility Service Listener -->
        <service
            android:name=".service.ObhoyAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="Obhoy Hardware SOS Listener"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- System Boot Receiver to Resume Background Monitoring -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Hardware Screen Toggle Fallback Receiver -->
        <receiver
            android:name=".receiver.ScreenToggleReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.SCREEN_ON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
